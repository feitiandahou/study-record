# React task board demo

This module demonstrates a minimal React state management flow:

- Use `useState` to manage task status.
- Derive board statistics from the current state.
- Render filtered task columns with reusable components.

## Run

```powershell
npm install
npm run dev
```

## Build

```powershell
npm run build
```