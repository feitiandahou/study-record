# React search gallery demo

This module demonstrates a minimal React async interaction flow:

- Use `useEffect` to simulate remote data loading.
- Use `useDeferredValue` to smooth filtering while typing.
- Render loading, empty, and result states clearly.

## Run

```powershell
npm install
npm run dev
```

## Build

```powershell
npm run build
```