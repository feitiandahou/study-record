# comprehension demo

This module demonstrates list and dict comprehensions:

- list comprehensions transform values concisely
- dict comprehensions build mappings from iterables
- filtering conditions can be embedded directly

## Run

```powershell
uv sync
uv run python -m app.main
```

The script prints squares and a parity lookup table.

## Test

```powershell
uv run pytest
```