# pathlib demo

This module demonstrates `pathlib` path operations:

- building paths with `/`
- reading path parts like stem and suffix
- writing code that is clearer than string concatenation

## Run

```powershell
uv sync
uv run python -m app.main
```

The script prints a summary for a sample path.

## Test

```powershell
uv run pytest
```