# Binary search demo

This module demonstrates a minimal binary search in Python:

- Search a sorted integer list.
- Return the index when found.
- Return `-1` when the target is missing.

## Run

```powershell
uv sync
uv run python -m app.main
```

## Test

```powershell
uv run pytest
```