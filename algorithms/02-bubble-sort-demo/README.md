# Bubble sort demo

This module demonstrates a minimal bubble sort in Python:

- Sort an integer list in ascending order.
- Stop early when a pass makes no swaps.
- Return a new sorted list without mutating the input.

## Run

```powershell
uv sync
uv run python -m app.main
```

## Test

```powershell
uv run pytest
```