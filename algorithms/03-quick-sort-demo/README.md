# Quick sort demo

This module demonstrates a minimal quick sort in Python:

- Use a recursive divide-and-conquer approach.
- Choose the first item as the pivot.
- Return a new sorted list without mutating the input.

## Run

```powershell
uv sync
uv run python -m app.main
```

## Test

```powershell
uv run pytest
```