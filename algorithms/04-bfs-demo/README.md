# Breadth-first search demo

This module demonstrates a minimal breadth-first search in Python:

- Search an unweighted graph level by level.
- Return the shortest path from start to target.
- Return an empty list when the target is unreachable.

## Run

```powershell
uv sync
uv run python -m app.main
```

## Test

```powershell
uv run pytest
```